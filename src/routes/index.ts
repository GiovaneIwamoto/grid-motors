/*
    contains the definitions of the API routes, i.e., the URLs that 
    the API supports and the corresponding HTTP methods for each route. 
*/

export { default as CarsRoute } from './CarsRoute';

/*
    contains the classes that represent the application's data models. 
    These classes usually correspond to tables in a database or other 
    data storage entities. Data models are used by repositories to perform 
    read and write operations in the database.
*/

export { default as carsSchema } from './Cars';
export { default as userSchema } from './Users';
export { default as reservesSchema } from './Reserves';
